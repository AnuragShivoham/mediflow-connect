{
  "name": "mediflow-connect-root",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "cd frontend && npx vite --host",
    "build": "cd frontend && npx vite build",
    "preview": "cd frontend && npx vite preview --host"
  }
}
